{
  "name": "sms-country-map",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data:build": "node scripts/build-data.mjs",
    "publish:arundas": "node scripts/publish-to-arundas-me.mjs"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "description": "Single-page world map of SMS sender support by country (AWS data)"
}
